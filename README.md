## Принятые решения

API `/items` не возвращает `id`, однако он требуется для получения детального объявления (`/items/:id`).

В рамках решения было принято использовать индекс элемента массива + 1 как `id`, так как сервер ожидает идентификаторы, начинающиеся с 1.